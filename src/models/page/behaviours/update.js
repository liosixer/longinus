import BaseBehaviour from "./base";

class UpdateBehaviour extends BaseBehaviour {
    constructor($name = "UPDATE", $label = "编辑", $code = 2){
        super($name, $label, $code);
    }
}

export default UpdateBehaviour;