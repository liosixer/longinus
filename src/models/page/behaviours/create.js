import BaseBehaviour from "./base";

class CreateBehaviour extends BaseBehaviour {
    constructor($name = "CREATE", $label = "新增", $code = 1){
        super($name, $label, $code);
    }
}

export default CreateBehaviour;